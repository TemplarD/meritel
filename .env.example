# === База данных ===
DB_HOST=localhost
DB_NAME=viz620
DB_USER=meritel
DB_PASSWORD=meritel123
DB_PORT=3306

# === Префикс таблиц ===
DB_PREFIX=viz620

# === Администратор (для восстановления) ===
# ADMIN_LOGIN=admin
# ADMIN_PASSWORD=ваш_пароль

# === Почта (для форм обратной связи) ===
MAIL_FROM=noreply@meritel43.ru
MAIL_TO=info@meritel43.ru

# === SMTP (опционально) ===
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=password

# === Секретный ключ (для сессий, CSRF) ===
APP_SECRET=your-secret-key-change-this-in-production

# === Окружение ===
APP_ENV=development
# APP_ENV=production

# === Отладка ===
DEBUG=false

# === Хост сайта ===
SITE_URL=http://localhost:8080
# SITE_URL=https://meritel43.ru
